[pytest]
asyncio_mode = auto
testpaths = tests
python_files = *_test.py
# aiohttp.BasicAuth 在当前 aiohttp 版本触发 DeprecationWarning；接口由计划冻结，此处仅静默这一第三方弃用，其它 warning 仍会显示。
filterwarnings =
    error::RuntimeWarning
    ignore:BasicAuth is deprecated and will be removed in aiohttp 4\.0:DeprecationWarning
