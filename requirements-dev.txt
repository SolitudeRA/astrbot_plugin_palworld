# 开发/CI 依赖（运行插件不需要；开发者执行 pip install -r requirements-dev.txt）
-r requirements.txt
PyYAML>=6.0          # 仅测试读取 metadata.yaml 用
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.15           # lint + import 排序（配置见 pyproject.toml [tool.ruff]）
mypy>=1.14           # 类型检查（配置见 pyproject.toml [tool.mypy]）
